// IWeapon.js
// Interface for all weapon types in the game.
// Note: JavaScript does not have interfaces, so we use JSDoc for documentation and enforce structure via BaseWeapon.

/**
 * @interface IWeapon
 * @property {number} ammoCount
 * @property {number} cooldownTime
 * @property {number} damageAmount
 * @method Fire(target)
 * @method Reload()
 */

// This file serves as documentation for the weapon interface.
// All weapons must implement the following methods and properties:
// - Fire(target)
// - Reload()
// - ammoCount
// - cooldownTime
// - damageAmount
